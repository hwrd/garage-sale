%h3{id: category.name.parameterize }= category.name

%table.table.table-sm.table-striped
  %thead.thead-inverse.table-hover
    %tr
      %th Identifier
      %th Description
      %th.text-xs-right Price

  %tbody
    - category.items.available.order(identifier: :asc).each do |item|
      %tr{class: ('unavailable' unless item.available?)}
        %td
          = item.identifier
        %td
          - unless item.images.empty?
            %i.fa.fa-picture-o
          = link_to item.description, item
        %td.text-xs-right
          = number_to_currency item.price


