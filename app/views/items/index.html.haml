%table.table
  %thead.thead-inverse.table-hover
    %tr
      %th Identifier
      %th Description
      %th Price
      %th Notes
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= item.identifier
        %td= item.description
        %td= item.price
        %td= item.notes
        %td= link_to 'Show', item

